#!/bin/bash
# Mac Health Checkup - Double-click to run
# This .command file opens in Terminal automatically when double-clicked.

# cd to the directory where this .command file lives
cd "$(dirname "$0")"

# Run the checkup script
./mac-checkup.sh
